# Normalize line endings to LF on checkout for all text files
* text=auto eol=lf

# Force binary files to stay binary (no line-ending conversion)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg text eol=lf
